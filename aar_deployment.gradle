task copyReleaseApk(type: Copy) {

    from 'build/outputs/aar', 'build/libs'

    into "C:/Users/ardak/Desktop/AARS"

    include '*debug.aar', '*sources.jar'
}

afterEvaluate {

    assembleDebug.finalizedBy(copyReleaseApk)
}